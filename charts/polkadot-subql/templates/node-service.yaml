apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-node
  labels:
    app: {{ .Release.Name }}-node
spec:
  ports:
  - name: service
    port: {{ .Values.node.config.port }}
  selector:
    app: {{ .Release.Name }}-node
